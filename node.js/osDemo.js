import os from 'os';

//userInfo() - gives info about currently logged user in the OS
console.log(os.userInfo());

//totalMem() - gives total memory in BYTES
console.log(os.totalmem());

//freemem() - gives total memory in BYTES

console.log(os.freemem());

//cpus() object with all cores !
console.log(os.cpus());
